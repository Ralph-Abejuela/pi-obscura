node_modules/
*.tsbuildinfo
# junctions pointing back into .agents/skills; git would duplicate them
.claude/
# research cache, not source
docs/.agent-cache/
# throwaway engine probes and verify evidence, not source
scratch/